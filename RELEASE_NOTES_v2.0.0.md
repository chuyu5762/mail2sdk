# Mail2 SDK v2.0.0

## 🎉 正式稳定版本发布

Mail2 SDK 是一个简洁易用的 Go 语言临时邮箱 SDK，用于与 Mail2 临时邮箱系统交互。

## ✨ 主要特性

- ✅ **单文件设计** - 只需复制 `mail2sdk.go` 到项目即可使用
- ✅ **多种邮箱生成模式** - 随机字符、中文拼音、英文名，支持自动混用
- ✅ **灵活的域名选择** - 支持指定域名、域名组随机选择、黑名单过滤
- ✅ **智能轮询策略** - 确保多个域名均匀使用，避免单一域名过载
- ✅ **完整的邮件操作** - 创建邮箱、获取邮件、提取验证码、删除邮箱
- ✅ **验证码提取** - 内置验证码提取功能，自动识别 4-8 位数字验证码
- ✅ **线程安全** - 支持并发调用，内置锁机制保证数据一致性

## 📦 安装使用

### 方式一：使用 go get（推荐）

```bash
go get github.com/chuyu5762/mail2sdk@v2.0.0
```

或获取最新版本：

```bash
go get github.com/chuyu5762/mail2sdk@latest
```

### 方式二：直接复制文件

```bash
# 下载 SDK 文件
curl -O https://raw.githubusercontent.com/chuyu5762/mail2sdk/v2.0.0/mail2sdk.go

# 或使用 wget
wget https://raw.githubusercontent.com/chuyu5762/mail2sdk/v2.0.0/mail2sdk.go
```

## 🚀 快速开始

```go
package main

import (
    "fmt"
    "log"
    "github.com/chuyu5762/mail2sdk"
)

func main() {
    baseURL := "https://mail.cwn.cc"
    apiKey := "your-api-key"

    // 创建临时邮箱
    mailbox, err := mail2sdk.CreateMailbox(baseURL, apiKey, mail2sdk.ModeRandom, "", nil)
    if err != nil {
        log.Fatal(err)
    }
    fmt.Printf("邮箱地址: %s\n", mailbox.Address)

    // 提取验证码
    result, err := mail2sdk.ExtractCode(baseURL, apiKey, mailbox.Address, 5)
    if err != nil {
        log.Fatal(err)
    }
    if result.Found {
        fmt.Printf("验证码: %s\n", result.Code)
    }
}
```

## 📚 核心 API

### 邮箱管理
- `CreateMailbox()` - 创建临时邮箱（支持 4 种生成模式）
- `CreateMailboxWithDomains()` - 从指定域名组创建邮箱
- `DeleteMailbox()` - 删除邮箱
- `GetDomains()` - 获取可用域名列表

### 邮件操作
- `GetMails()` - 获取邮件列表
- `GetMailDetail()` - 获取邮件详情（含完整内容）
- `ExtractCode()` - 自动提取验证码

### 辅助功能
- `GetDomainStats()` - 获取域名使用统计
- `ResetDomainStats()` - 重置统计计数

## 📖 文档

完整文档请查看：[README.md](https://github.com/chuyu5762/mail2sdk/blob/main/README.md)

## 🎯 应用场景

- ✅ 自动化测试（注册流程、邮件验证）
- ✅ 批量邮箱创建
- ✅ 验证码自动获取
- ✅ 临时邮箱服务集成

## 🔧 技术细节

- **Go 版本要求**：1.21+
- **依赖**：仅标准库，零第三方依赖
- **线程安全**：全部 API 支持并发调用
- **域名策略**：智能轮询算法，确保负载均衡

## ⚠️ 注意事项

1. 需要有效的 Mail2 API Key
2. 临时邮箱有过期时间，请及时处理
3. 注意 API 速率限制
4. 验证码提取仅支持 4-8 位纯数字

## 🐛 已知问题

无

## 📝 更新日志

### v2.0.0 (2025-11-07)

#### 重要变更
- 🎉 正式稳定版本发布
- 📝 完整的中文文档
- ✅ 支持 Go Module 安装（`go get`）
- 🔧 修复版本号管理

#### 功能列表
- 邮箱创建（4 种模式：自动混用、随机字符、中文拼音、英文名）
- 域名选择和黑名单过滤
- 邮件获取和详情查看
- 验证码自动提取
- 邮箱删除
- 智能域名轮询策略
- 线程安全设计

## 🙏 致谢

感谢使用 Mail2 SDK！

如有问题或建议，欢迎提交 [Issue](https://github.com/chuyu5762/mail2sdk/issues)。

## 📄 许可证

MIT License

---

**项目地址**：https://github.com/chuyu5762/mail2sdk  
**文档地址**：https://github.com/chuyu5762/mail2sdk/blob/main/README.md
